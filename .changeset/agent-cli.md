---
"@bjacobso/triplex-cli": minor
---

Add an Effect v4 CLI with stable JSON commands for exploring entities, facts, Datalog, journals,
and complete configuration history, plus attributed transaction writes and atomic ref moves over
SQLite or PostgreSQL.
